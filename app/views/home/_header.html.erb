<nav class="navbar fixed-bottom navbar-expand-lg navbar-dark" id="nav-bar">
  <div class="container-fluid d-flex justify-content-around">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div>
          <h6> Dont't miss what's happening </h6>
          <p> People on Twitter are the first to know. </p>
        </div>
        <div>
          <%= link_to "Log in", new_user_session_path, class: "btn btn-outline-light" %>
          <%= link_to "Sign up", new_user_registration_path, class: "btn btn-light" %>
        </div>
<!--
        <% if user_signed_in? %> 
          <form class="d-flex" role="search">
              <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
              <button class="btn btn-dark" type="submit">Search</button>
          </form>
          <div class="d-flex justify-content-center align-items-center">
              <i class="fas fa-comments me-3"></i>
              <i class="fas fa-bell me-3"></i>
              <div class="dropdown me-3">
                <i class="fas fa-info" type="button" data-bs-toggle="dropdown" aria-expanded="false"></i>  insert user image
                <ul class="dropdown-menu dropdown-menu-custom">
                  <li>
                    <%= link_to "View Profile", profiles_path, class:"dropdown-item" %>
                  </li>
                  <li>
                    <%= link_to "Sign Out", destroy_user_session_path, method: :delete, data:{ turbo_method: :delete}, class:"dropdown-item" %>
                  </li>
                  <li>
                    <%= link_to "Delete Account", edit_user_registration_path, class:"dropdown-item" %> to be relocated to user profile
                  </li>         
                </ul>
              </div>            
          </div>
        <% end %> -->
  </div>
</nav>